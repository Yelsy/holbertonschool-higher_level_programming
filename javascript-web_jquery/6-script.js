$(document).ready(function() {
    // Add a click event handler to the DIV
    $('#update_header').click(function() {
      // Select the header element using the header and update its text
      $('header').text('New Header!!!');
    });
  });
